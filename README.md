#MY PYTHON - A python intrepreter written in c++

This repo is mini-interpreter of Python.

###Compile
```bash run.sh ```
The script compiles the cpp file and copies it to ```/bin``` directory. Make sure you have /bin already

